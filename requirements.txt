# Carnets de Sanidad - dependencias de Python
# Instalar con: pip install -r requirements.txt

streamlit==1.60.0
pandas==3.0.2
openpyxl==3.1.5
docxtpl==0.20.2
python-docx==1.2.0
docxcompose==2.2.0
Pillow==12.1.1

# Pruebas
pytest==9.1.1

# Nota: la generacion de PDF requiere LibreOffice instalado en el sistema
# (no es un paquete de Python). Ver README.md, seccion "Requisitos del
# sistema", para instrucciones de instalacion.
